export class NoxTreeDataHelper {
    static convertTableToObject(data: any[]): any {
        const result: any = {};

        for (let i = 0; i < data.length; i++) {
            
        }

        return result;
    }
}